package Operation;

import org.openjasmine.types.Matrix;
import org.openjasmine.types.VectoR;

public class NumAnalysis {	
	public static VectoR gaussElm(Matrix A, VectoR b) {
		VectoR result = new VectoR(b.getSize());
		return result;
	}
}
